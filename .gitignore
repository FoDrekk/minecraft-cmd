# The SQLite fallback database, at the project root only.
# Anchored with a leading slash: an unanchored "data/" also matches
# lib/data/, which silently excluded the whole content library.
/data/

# Test harness dependencies (see package.json).
/node_modules/

# Python bytecode cache (see python/minecraft_engine/).
__pycache__/
*.pyc
